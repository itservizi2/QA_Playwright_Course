// Example: Iterate through an array using forEach
const languages: string[] = ["English", "Italian", "Spanish", "French", "German"];

console.log("Iterating through the array:");
languages.forEach((lang, index) => {
    console.log(`Index ${index}: ${lang}`);
});

